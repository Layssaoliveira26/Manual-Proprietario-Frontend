services:
  frontend:
    build: .
    container_name: pi-frontend
    ports:
      - '5173:5173'
    volumes:
      #sincroniza o código local com o container, para caso de alterações
      - .:/app
      #Preserva node_modules do container
      - /app/node_modules
    env_file:
      - .env
